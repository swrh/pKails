<%= form_with(model: prospection, local: true) do |form| %>
  <% if prospection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prospection.errors.count, "error") %> prohibited this prospection from being saved:</h2>

      <ul>
      <% prospection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id, id: :prospection_customer_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :prospection_product_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :prospection_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
